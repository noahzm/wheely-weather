---
paths:
  - 'src/components/**'
  - 'src/app/**'
---

**UI change:** locate the exact component/screen and adjacent primitives first; enforce theme-token usage (no inline color literals); list touched files before editing and keep edits surgical; validate with `npm run lint` plus the smallest relevant tests.
