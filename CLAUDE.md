# CLAUDE.md

This repository's canonical AI-assistant instructions (commands, architecture, conventions, and quality gates) live in [`AGENTS.md`](./AGENTS.md) — read it before working here.

Task-specific playbooks (UI changes, domain/utility logic changes, platform-specific service changes) live in `.claude/rules/` and load automatically when working under their matching paths. The same playbooks are mirrored in `.github/copilot-instructions.md`.
